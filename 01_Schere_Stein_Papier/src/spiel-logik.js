// Konstanten fuer erlaubte Züge
const MOVES = ["schere", "stein", "papier"]

/**
 * Liefert den Zug des Computers.
 * TODO: Implementiere zufaellige Auswahl aus MOVES
 */
function getComputerMove () {
    // Tipp: Math.random() und Math.floor() verwenden
    throw new Error('TODO: getComputerMove noch nicht implementiert')
}


/**
 * Entscheidet, ob der Spieler gewonnen, verloren oder unentschieden hat.
 * Rueckgabe: "gewonnen" | "verloren" | "unentschieden"
 * TODO: Vergleiche player und computer gemäss Schere>Papier, Papier>Stein, Stein>Schere
 */
function decideWinner (player, computer) {
    // Tipp: erst Eingaben pruefen, dann Gleichheit, dann Regelwerk abbilden
    throw new Error('TODO: decideWinner noch nicht implementiert')
}